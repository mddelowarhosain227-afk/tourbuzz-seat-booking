plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.tourbuzz_seat_booking"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.tourbuzz_seat_booking"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }
}

flutter {
    source '../..'
}
